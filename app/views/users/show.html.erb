<h1><%= "Hello, #{@user.name}." %> </h1>

  <% if @user.site_admin? %>
    <p>What would you like to do today?
      <%= link_to "Create a new site", new_site_path(current_user, @site) %><br>
      <p>Update one of your existing sites:</p>
      <% current_user.sites.each do |site| %>
      <%= image_tag site.image.url(:medium) %>
      <%= link_to "View or update this site", site_path(current_user, @site) %>
      <%end%>

  <% else %>
    <h2>Here is your Research Gallery.</h2>
  <% end %>
