<h1><%= "Hello, #{@user.name}." %> </h1>


    <p>What would you like to do today?
      <%= link_to "Create a new site", new_user_site_path(current_user, @site) %><br>
      <p>Update one of your existing sites:</p>
      <% current_user.sites.each do |site| %>
      <%= image_tag site.image.url(:medium) %><br>
      <%= link_to "View or update this site", user_site_path(current_user, @site) %><br>
      <%end%>
